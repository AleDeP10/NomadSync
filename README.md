# ObsidianSync
Java CLI tool that automates Git-based sync of an Obsidian vault across Windows machines. Pull on logon, push on logoff, differential autosave in between. Event-driven, retry with exponential backoff, Task Scheduler integration.
